// import { ZodType, z } from "zod";

export type discussionData = {
    id: number,
    topic: "string",
    degree_of_importance: number,
    is_terminated: boolean,
    is_answered: boolean,
    department: ""
};

